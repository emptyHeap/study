const http = require('http');
const https = require('https');
const path = require('path');

class Router {
  constructor(prefix = '') {
    this.prefix = prefix;
  }
}

class Server {
  constructor(config) {
    const {protocol} = config;
  }


}
